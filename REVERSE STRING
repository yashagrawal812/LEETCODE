class Solution {
    public void reverseString(char[] s) {
        int l=s.length;
        char c;
        for(int i=l-1,j=0;i>j;i--,j++)
        {
            c=s[j];
            s[j]=s[i];
            s[i]=c;
        }
    }
}
